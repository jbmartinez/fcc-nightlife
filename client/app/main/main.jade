.main.group
  .header(ng-class="{true: 'is-submitted', false: ''}[formSubmitted===true]")
    form(ng-submit="search()") I'm in 
      input(type="text", name="search", id="search" ng-model="location" autofocus)
      i.fa.fa-search.icon-search(ng-click="search()")
  .results.ng-cloak
    .card(ng-repeat="venue in venues")
      img(ng-src="{{venue.photo}}")
      a(href="{{venue.link}}", target="_blank")
        h3 {{venue.name}}
      .count(ng-click="toggleCount($index)" ng-class="{true: 'going', false: ''}[venue.who.indexOf(userID) >= 0]")
        i.fa.fa-hand-o-up.icon
        | {{venue.who.length || 0}}

    footer.footer(ng-class="{true: '', false: 'hidden'}[formSubmitted===true]")
      .container
        p
          | Demo built by 
          = ' | '
          a(href='https://twitter.com/_jmartinez')
            i.fa.fa-twitter
            @_jmartinez
          = ' | '
          a(href='https://github.com/jbmartinez/fcc-nightlife') GitHub
          = ' | ' 
          | results powered by Foursquare
          